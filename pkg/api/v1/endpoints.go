package v1

// Base path for this API version
const (
	BasePath string = "/v1"
)

// Supported v1 API paths
const (
	PathColleges string = "/colleges"
	PathTerms    string = "/terms"
	PathSubjects string = "/subjects"
	PathCourses  string = "/courses"
	PathSections string = "/sections"
)
